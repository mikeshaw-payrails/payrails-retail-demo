# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build outputs (server)
server/dist
server/build

# TypeScript incremental build info
*.tsbuildinfo

# Environment & secrets (example allowlist in README instead)
.env
.env.*
!.env.example

# Coverage
coverage
*.lcov

# Cache / tool artifacts
.eslintcache
.stylelintcache
.parcel-cache
.vite
/.turbo
/.bun

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# OS / Editor
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# Archives
*.tgz

# Optional lockfiles (keep the one your team uses)
pnpm-lock.yaml
package-lock.json
yarn.lock

# Storybook / design system (if added later)
storybook-static

# Jest / Vitest
jest-cache/
.jest/
*.jest-runner.json

# Temporary
tmp/
temp/
.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
